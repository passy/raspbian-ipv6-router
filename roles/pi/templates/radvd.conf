interface eth0 {
    AdvSendAdvert on;
    IgnoreIfMissing on;
    AdvDefaultPreference high;

    prefix {{eth0.prefix6}}/64 {
        AdvOnLink on;
        AdvAutonomous on;
    };

    DNSSL lan {
    };

    RDNSS {{eth0.address6}} {
    };
};
